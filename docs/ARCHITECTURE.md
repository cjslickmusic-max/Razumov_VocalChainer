# Архитектура (черновик)

Пока реализован минимальный **passthrough** (`Source/PluginProcessor.cpp`) и заглушка UI (`Source/PluginEditor.cpp`).

После **Фазы 1** (`docs/ROADMAP.md`) здесь появится:

- схема графа узлов и поток `processBlock`;
- модель задержки и merge параллельных веток;
- связь APVTS с узлами.
